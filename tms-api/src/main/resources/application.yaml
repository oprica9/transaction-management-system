spring:
  application:
    name: tms-api

tms:
  storage:
    csv-path: ${TMS_CSV_PATH:${user.home}/tms/transactions.csv}
  cors:
    allowed-origins:
      - ${TMS_CORS_ALLOWED_ORIGINS:http://localhost:5173}
